[pytest]
markers =
    integration: Tests requiring DB / external calls side-effects
    slow: Potentially slower tests (network, long timers)
    plugin_all: Full sweep of every plugin function including edge cases
    ai: AI integration tests
addopts = -ra
